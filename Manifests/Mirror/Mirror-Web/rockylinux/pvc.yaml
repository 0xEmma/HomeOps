apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mirror-rockylinux
  namespace: default
  labels:
    app: mirror-rockylinux
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storageClassName: csi-fs-hdd-sc
      storage: 3000Gi
