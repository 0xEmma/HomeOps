apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mirror-kali
  namespace: default
  labels:
    app: mirror-kali
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 700Gi
